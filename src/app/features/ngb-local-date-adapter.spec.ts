import { NgbLocalDateAdapter } from './ngb-local-date-adapter';

describe('NgbLocalDateAdapter', () => {
  it('should create an instance', () => {
    expect(new NgbLocalDateAdapter()).toBeTruthy();
  });
});
