import { ActivityDatasource } from './activity.datasource';

describe('ActivityDatasource', () => {
  it('should create an instance', () => {
    expect(new ActivityDatasource()).toBeTruthy();
  });
});
