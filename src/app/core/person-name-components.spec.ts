import { PersonNameComponents } from './person-name-components';

describe('PersonNameComponents', () => {
  it('should create an instance', () => {
    expect(new PersonNameComponents()).toBeTruthy();
  });
});
