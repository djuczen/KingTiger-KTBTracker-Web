import { CycleCandidateComponent } from './cycle-candidate-component';

describe('CycleCandidateComponent', () => {
  it('should create an instance', () => {
    expect(new CycleCandidateComponent()).toBeTruthy();
  });
});
